server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/jingjitree/SpringcloudConfig
          searchPaths: respo
#          username:
#          password:
      label: master
